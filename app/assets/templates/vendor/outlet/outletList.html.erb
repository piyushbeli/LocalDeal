<h3>Outlets</h3>

<div class="row" ng-repeat="outlet in vendor.outlets">
  <a ui-sref="app.outletDetail({id: outlet.id})" href="javascript:void(0)">{{outlet.name}}</a>
</div>
<button class="btn btn-primary" ui-sref="app.newOutlet">Add Outlet</button>
