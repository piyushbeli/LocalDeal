<nav class="navbar navbar-default">
    <div class="container">
        <div class="collapse navbar-collapse pull-right" ng-controller="NavController">
            <ul class="nav navbar-nav">
              <li ng-show="isLoggedIn()"><a ui-sref="app.accountDetail">{{ user.name || user.email }}</a></li>
              <li ng-show="isLoggedIn()"><a class="btn" href="javascript:void(0)" ng-click="doLogout()">Log Out</a></li>
            </ul>
        </div>
    </div>
</nav>
<div class="container">
    <div ui-view="mainContent"></div>
</div>